name: "C300X CodeQL config"

paths-ignore:
  # Qt/QML files and their QML JavaScript libraries are valid for the device
  # runtime but are not valid ECMAScript/TypeScript for CodeQL's JS parser.
  - "device_qml/**"
  - "custom_components/bticino_c300x/device_agent/qml/**"
