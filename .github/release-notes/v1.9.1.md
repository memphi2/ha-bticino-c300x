# BTicino C300X Integration v1.9.1

## Unreleased

- Fix native-agent update metadata after v1.9.0 shipped a changed agent binary
  under the previous visible agent version. Future releases now fail validation
  if native-agent or bundled device files change without a matching
  `native_agent/VERSION` bump.
