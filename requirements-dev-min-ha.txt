# Direct validation dependencies for the minimum Home Assistant matrix. CI
# installs Home Assistant separately, so this file intentionally does not pin
# Home Assistant transitive dependencies.
av==18.0.0
mypy==2.1.0
numpy==2.5.1
paramiko==3.5.1
PyTurboJPEG==2.4.0
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.15.20
