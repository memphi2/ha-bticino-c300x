# Direct validation dependencies. CI uses pinned matrix files. Dependabot updates
# requirements-dev.txt; requirements-dev-min-ha.txt is curated manually because
# it must stay compatible with Home Assistant's minimum-version transitive pins.
# Home Assistant itself is locked per CI matrix.
av==18.0.0
mypy==2.3.0
numpy==2.5.1
paramiko==3.5.1
PyTurboJPEG==2.5.0
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.0
